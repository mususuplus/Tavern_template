# fur 开场白定稿规则

rule:

- 必须参考已保存的 `<fur_world>`，以及已保存的 `<fur_character>` 或 `<fur_simulator>`。
- 开场白要能让 <user> 自然接话，不能只是设定说明。
- 输出必须包含 `<fur_opening>...</fur_opening>`。

format: |-
  <fur_opening>
    <可直接放入角色卡第一条消息的正文>
  可接话方向:
    - <user可选择的回应方向1>
    - <user可选择的回应方向2>
  </fur_opening>
